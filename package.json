{
  "name": "factory-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen --output-dir src/types/",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ erc20-factory",
    "create-local": "graph create --node http://localhost:8020/ erc20-factory",
    "remove-local": "graph remove --node http://localhost:8020/ erc20-factory",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 erc20-factory"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.25.3",
    "@graphprotocol/graph-ts": "0.24.1",
    "@typescript-eslint/eslint-plugin": "^5.9.1",
    "@typescript-eslint/parser": "^5.9.1",
    "eslint": "^8.7.0",
    "eslint-config-prettier": "^8.3.0",
    "prettier": "^2.5.1",
    "typescript": "^4.5.4"
  }
}
